# XSmall-HMI-SCADA_input_output
Библиотека готовых компонентов ввода-вывода (кнопки, поля, дисплеи) для быстрой разработки графического интерфейса (HMI) в системах SCADA для платформы XSmall-HMI. Интерфейс описывается в простых XML-файлах, а библиотека динамически создаёт интерактивные элементы с помощью SFML.

🚀 Возможности
📝 Декларативная конфигурация через XML: Полностью настраиваемый интерфейс через файлы конфигурации.

🎯 Три типа готовых виджетов:

Кнопка (Button): Интерактивная кнопка с визуальными состояниями (наведение, нажатие) и кастомными обработчиками.

Поле ввода (TextField): Текстовое поле с поддержкой ввода, фокусом, автоподбором размера шрифта и защитой от переполнения.

Текстовый дисплей (TextDisplay): Поле для вывода статических или динамически обновляемых значений.

⚙️ Объектно-ориентированная архитектура: Базовый класс Widget с виртуальными методами для единообразного управления всеми элементами.

🔄 Поддержка русского языка: Корректная обработка кириллицы в интерфейсе и текстовых полях.

✅ Встроенный парсер XML: Простой и понятный парсер конфигурационных файлов с проверкой ошибок.

Инструкция по сборке проекта SCADA/HMI
Предварительные требования:
1. CMake версии 3.18 или выше
2. Git для загрузки зависимостей
3. Компилятор C++ с поддержкой C++17

Windows: Visual Studio (2019 или новее) или MinGW-w64

Linux: GCC 8+ или Clang 7+

macOS: Xcode 11+ или Clang

Сборка проекта
Windows
Способ 1: Visual Studio
```bash
# 1. Клонирование репозитория (если нужно)
git clone https://github.com/SHIvan13/XSmall-HMI-SCADA_input_output.git
cd XSmall-HMI-SCADA_input_output

# 2. Создание папки для сборки
mkdir build
cd build

# 3. Генерация проекта Visual Studio
cmake .. -G "Visual Studio 17 2022" -A x64

# 4. Открытие решения в Visual Studio
# ИЛИ сборка через командную строку
cmake --build . --config Release
```
Способ 2: MinGW (MSYS2)
```bash
# 1. Установка CMake и компилятора через MSYS2
pacman -S mingw-w64-x86_64-cmake mingw-w64-x86_64-gcc

# 2. Генерация и сборка
mkdir build && cd build
cmake .. -G "MinGW Makefiles"
cmake --build .
```
Linux
```bash
# 1. Установка зависимостей (Ubuntu/Debian)
sudo apt-get update
sudo apt-get install -y cmake g++ git

# 2. Сборка проекта
mkdir build && cd build
cmake .. -DCMAKE_BUILD_TYPE=Release
make -j$(nproc)
```
macOS
```bash
# 1. Установка Homebrew (если нет)
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"

# 2. Установка CMake
brew install cmake

# 3. Сборка проекта
mkdir build && cd build
cmake .. -DCMAKE_BUILD_TYPE=Release
make -j$(sysctl -n hw.ncpu)
```
Запуск приложения
Windows
```bash
# После сборки
cd build
./SCADA.exe
```
Linux/macOS
```bash
cd build
./SCADA
```
